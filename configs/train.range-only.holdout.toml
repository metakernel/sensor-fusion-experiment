[train]
run_name = "range_holdout_seed42"
batch_size = 8
learning_rate = 0.001
epochs = 3
seed = 42

[dataset]
config = "configs/dataset.waymo.small.holdout.toml"

[model]
config = "configs/model.range-only.tiny.toml"

