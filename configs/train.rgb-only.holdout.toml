[train]
run_name = "rgb_holdout_seed42"
batch_size = 4
learning_rate = 0.001
epochs = 3
seed = 42

[dataset]
config = "configs/dataset.waymo.small.holdout.toml"

[model]
config = "configs/model.rgb-only.tiny.toml"

