[train]
run_name = "fusion_run_001"
batch_size = 8
learning_rate = 0.001
epochs = 3
seed = 42

[dataset]
config = "configs/dataset.waymo.small.toml"

[model]
config = "configs/model.fusion.tiny.toml"
